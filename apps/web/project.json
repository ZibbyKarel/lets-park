{
  "name": "web",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/web",
  "projectType": "application",
  "tags": ["type:app", "scope:web"],
  "// targets": "to see all targets run: nx show project web --web",
  "targets": {
    "build": {
      "// env": "Nx injects the workspace-root and project-root `.env` files into every target's environment. Both carry NODE_ENV=development (they are dev env files — see doc/decision/0009-*), which would otherwise leak into `next build` and produce a mixed development/production React resolution. Pin it here; see doc/decision/0028-*.",
      "options": {
        "env": {
          "NODE_ENV": "production"
        }
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "tsc --noEmit -p apps/web/tsconfig.json",
          "tsc --noEmit -p apps/web/tsconfig.spec.json"
        ],
        "parallel": false
      }
    }
  }
}
