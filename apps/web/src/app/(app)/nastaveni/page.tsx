import { SectionPlaceholder } from '../../../shell/section-placeholder';

/**
 * Personal settings: licence plate, preferred parking spot, and the ICS feed
 * URL with its regenerate button. Task 27 builds it; this task routes the
 * avatar menu's `Nastavení (SPZ auta)` entry here.
 */
export default function SettingsPage() {
  return <SectionPlaceholder section="settings" />;
}
