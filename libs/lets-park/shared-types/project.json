{
  "name": "shared-types",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/lets-park/shared-types/src",
  "projectType": "library",
  "tags": ["type:util", "scope:shared", "layer:foundation", "app:lets-park"],
  "// targets": "to see all targets run: nx show project shared-types --web",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "tsc --noEmit -p libs/lets-park/shared-types/tsconfig.lib.json",
          "tsc --noEmit -p libs/lets-park/shared-types/tsconfig.spec.json"
        ],
        "parallel": false
      }
    }
  }
}
