{
  "name": "form",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/shared/form/src",
  "projectType": "library",
  "tags": ["type:util", "scope:web", "app:shared"],
  "// targets": "to see all targets run: nx show project form --web",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "tsc --noEmit -p libs/shared/form/tsconfig.lib.json",
          "tsc --noEmit -p libs/shared/form/tsconfig.spec.json"
        ],
        "parallel": false
      }
    }
  }
}
