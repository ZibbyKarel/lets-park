{
  "name": "api-client",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/shared/api-client/src",
  "projectType": "library",
  "tags": ["type:util", "scope:web"],
  "// targets": "to see all targets run: nx show project api-client --web",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "tsc --noEmit -p libs/shared/api-client/tsconfig.lib.json",
          "tsc --noEmit -p libs/shared/api-client/tsconfig.spec.json"
        ],
        "parallel": false
      }
    }
  }
}
