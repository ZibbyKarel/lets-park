{
  "name": "query",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/query/src",
  "projectType": "library",
  "tags": ["type:util", "scope:web"],
  "// targets": "to see all targets run: nx show project query --web",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "tsc --noEmit -p libs/query/tsconfig.lib.json",
          "tsc --noEmit -p libs/query/tsconfig.spec.json"
        ],
        "parallel": false
      }
    }
  }
}
